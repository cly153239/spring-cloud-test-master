spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      name: application
      profile: dev
#      uri: http://localhost:8083/
      discovery:
        enabled: true
        service-id: config-server

server:
  port: 8084
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/
#management:
#  security:
#    enabled: false