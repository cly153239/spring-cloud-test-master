spring:
  application:
    name: sentinel-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    sentinel:
      transport:
        dashboard: 127.0.0.1:8080   #sentinel控制台的请求地址
server:
  port: 8104
